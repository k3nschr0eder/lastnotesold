import { createFileRoute, Link, notFound } from "@tanstack/react-router";
import { getPostBySlug } from "~/lib/blog";

export const Route = createFileRoute("/blog/$slug")({ loader: async ({ params }) => { const post = await getPostBySlug(params.slug); if (!post) throw notFound(); return post; }, head: ({ loaderData }) => ({ meta: loaderData ? [{ title: `${loaderData.title} - LastNoteSold` }, { name: "description", content: loaderData.description }] : [] }), component: ArticlePage });
function date(value: string) { return new Date(value + "T12:00:00").toLocaleDateString("en-US", { month: "long", day: "numeric", year: "numeric" }); }
function Markdown({ body }: { body: string }) { return <div className="space-y-5 text-lg leading-relaxed text-gray-300">{body.split("\n\n").map((block, i) => block.startsWith("## ") ? <h2 key={i} className="pt-5 text-2xl font-bold text-white">{block.slice(3)}</h2> : <p key={i}>{block}</p>)}</div>; }
function ArticlePage() { const post = Route.useLoaderData(); return <div className="pt-16 sm:pt-20"><article className="mx-auto max-w-3xl px-4 py-12 sm:px-6 sm:py-20"><Link to="/blog" className="text-sm font-semibold text-emerald-400 hover:text-emerald-300">← Back to the blog</Link><div className="mt-10"><div className="flex flex-wrap items-center gap-3 text-xs"><span className="rounded-full bg-emerald-900/60 px-3 py-1 font-semibold text-emerald-300">{post.category}</span><span className="text-gray-500">{date(post.published_at)} · {post.read_time}</span></div><h1 className="mt-5 text-4xl font-extrabold tracking-tight text-white sm:text-5xl">{post.title}</h1><p className="mt-5 text-xl leading-relaxed text-gray-400">{post.description}</p></div><div className="my-10 h-1 rounded-full bg-gradient-to-r from-emerald-600 to-transparent" /><Markdown body={post.body} /></article></div>; }

import { createFileRoute, Link } from "@tanstack/react-router";
import { getAllPosts, getFeaturedPosts, type BlogPost } from "~/lib/blog";

export const Route = createFileRoute("/blog")({ loader: async () => ({ posts: await getAllPosts(), featured: await getFeaturedPosts() }), head: () => ({ meta: [{ title: "Blog - LastNoteSold" }, { name: "description", content: "Paper money collecting insights, market trends, and pricing tips from LastNoteSold." }] }), component: BlogPage });
function date(value: string) { return new Date(value + "T12:00:00").toLocaleDateString("en-US", { month: "long", day: "numeric", year: "numeric" }); }
function PostCard({ post }: { post: BlogPost }) { return <article className="group overflow-hidden rounded-2xl border border-gray-800 bg-gray-900/60 transition hover:-translate-y-1 hover:border-emerald-700/60"><div className="h-32 bg-gradient-to-br from-emerald-950 via-gray-900 to-gray-800 sm:h-40"><div className="flex h-full items-center justify-center text-5xl opacity-50">💵</div></div><div className="p-5"><p className="text-xs font-semibold uppercase tracking-wider text-emerald-400">{post.category}</p><h2 className="mt-2 text-xl font-bold text-white group-hover:text-emerald-300">{post.title}</h2><p className="mt-2 text-sm leading-relaxed text-gray-400">{post.description}</p><p className="mt-4 text-xs text-gray-500">{date(post.published_at)} · {post.read_time}</p></div></article>; }
function BlogPage() { const { posts, featured } = Route.useLoaderData(); const feed = posts.filter(p => !featured.some(f => f.slug === p.slug)); return <div className="pt-16 sm:pt-20"><section className="border-b border-emerald-900/20 bg-gray-900/30 py-14 sm:py-20"><div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><p className="text-sm font-semibold uppercase tracking-[0.2em] text-emerald-400">The LastNoteSold Blog</p><h1 className="mt-3 max-w-3xl text-4xl font-extrabold tracking-tight text-white sm:text-6xl">Notes worth knowing.</h1><p className="mt-5 max-w-2xl text-lg text-gray-400">Paper money stories, market insights, and practical pricing advice for collectors and live streamers.</p><div className="mt-10 grid gap-5 lg:grid-cols-2">{featured.map(post => <Link key={post.slug} to="/blog/$slug" params={{ slug: post.slug }} className="group rounded-2xl border border-emerald-800/40 bg-gray-950/70 p-6 transition hover:border-emerald-500/70"><div className="flex items-center gap-3 text-xs"><span className="rounded-full bg-emerald-900/60 px-3 py-1 font-semibold text-emerald-300">{post.category}</span><span className="text-gray-500">{date(post.published_at)} · {post.read_time}</span></div><h2 className="mt-5 text-2xl font-bold text-white group-hover:text-emerald-300 sm:text-3xl">{post.title}</h2><p className="mt-3 text-gray-400">{post.description}</p><span className="mt-5 inline-block text-sm font-semibold text-emerald-400">Read article →</span></Link>)}</div></div></section><section className="py-14 sm:py-20"><div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><div className="mb-8 flex items-end justify-between"><div><p className="text-sm font-semibold uppercase tracking-wider text-emerald-400">From the archive</p><h2 className="mt-2 text-3xl font-bold text-white">Latest articles</h2></div></div>{feed.length ? <div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">{feed.map(post => <Link key={post.slug} to="/blog/$slug" params={{ slug: post.slug }}><PostCard post={post} /></Link>)}</div> : <p className="rounded-xl border border-gray-800 p-8 text-gray-400">More notes are on the way.</p>}</div></section><section className="border-t border-emerald-900/20 bg-emerald-950/20 py-14 text-center"><h2 className="text-2xl font-bold text-white sm:text-3xl">Ready to price banknotes faster on stream?</h2><Link to="/pricing" className="mt-6 inline-block rounded-lg bg-emerald-600 px-6 py-3 font-semibold text-white transition hover:bg-emerald-500">Try LastNoteSold Free →</Link></section></div>; }
